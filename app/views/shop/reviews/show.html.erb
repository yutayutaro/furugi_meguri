<%= link_to '一覧へ戻る',shop_reviews_path,class: "btn btn-primary" %>
<% if @review.customer.is_deleted == false%>
<h1 class="h1-text">レビューshow</h1>
<table class="table">
 <tbody>
  <tr>
    <th><%= "投稿者名"%></th>
    <td><%=@review.customer.nickname%></td>
  </tr>     
  <tr>
    <th><%= "タイトル"%></th>
    <td><%=@review.title%></td>
  </tr>     
  <tr>
    <th><%= "説明"%></th>
    <td><%=@review.introduction%></td>
  </tr>     
  <tr>
    <th><%= "画像"%></th>
    <% if @review.image.attached? %>
    <td><%= image_tag @review.image %></td>
    <% else %>
    <td><%= "画像はありません"%></td>
  </tr>  
    <% end %>
  <tr>
    <th><%= "投稿日時"%></th>
    <td><%= @review.created_at.strftime("%Y/%m/%d")%></td>
  </tr>     
 </tbody>
</table>
<%=link_to '削除する', shop_review_path(params[:id]),method: :delete,class: "btn btn-danger" %>
<% else %>
<h1 class="h1-text">削除済みのアカウントです。</h1>
<% end %>